# NOTE: this CLI tool currently has no third-party dependencies.
# - argparse was removed: it is part of the Python standard library.
#   The PyPI package "argparse==1.4.0" is an unmaintained backport from 2015
#   and should never be installed on modern Python.
# - Flask was not used anywhere in the code. If you plan to add a web
#   interface, uncomment the line below (>=3.1.3 avoids known CVEs in
#   older Flask/Werkzeug versions, e.g. CVE-2023-30861 session-caching leak).
# Flask>=3.1.3
